---
name: useState
route: /usestate
---
import Editor from '../components/Editor'
import {
    DeclareStateVar, 
    UpdateStateVar, 
    MultipleStateVars, 
    StateObject, 
    StateFromFn,
    CounterFnSetState
} from '../components/examples/useState'


# useState 🦄
 
Примечания:
- `useState` позволяет использовать локальное состояние внутри функционального компонента.
- Сигнатура вызова: `const [state, stateUpdaterFunction] = useState(initialStateValue)`
- [Документация](https://reactjs.org/docs/hooks-reference.html#usestate)
--- 

## Объявление переменной состояния
Объявление переменной состояния осуществляется вызовом `useState` с некоторым начальным значением состояния.
<Editor code={DeclareStateVar}/>

## Обновление переменной состояния
Обновить переменную состояния можно вызовом функции обновления, возвращенной вызовом `useState`.

```js
  const [stateValue, updaterFn] = useState(initialStateValue);
```
<Editor code={UpdateStateVar} />


## Несколько переменных состояния
Можно использовать и обновлять несколько переменных состояния функционального компонента, так как показано ниже:

<Editor code={MultipleStateVars} />


## Использование объекта в качестве переменной состояния
Вместо строки или числа вы можете использовать объект в качестве начального значения, передаваемого в useState.
> Обратите внимание: вы должны передать весь объект состояния в функцию обновления useState, поскольку объект заменяется, а НЕ объединяется.

<Editor code={StateObject} />

```js
// 🐢 setState (объединение объектов) против useState (замена объекта)
// предполагаем, что начальное состояние - {name: "Ohans"}

setState({ age: 'unknown' })
// новый объект состояния будет
// {name: "Ohans", age: "unknown"}

useStateUpdater({ age: 'unknown' })
// новый объект состояния будет
// {age: "unknown"} - изначальный объект состояния заменился

```
  

## Инициализация состояния из функции
Вместо простой передачи значения начального состояния, состояние также можно инициализировать из функции, как показано ниже:

<Editor code={StateFromFn} />


## Функциональный `setState`
Функция обновления, возвращаемая при вызове `useState`, также может принимать функцию, аналогично старому доброму `setState`.

```js
 const [value, updateValue] = useState(0)
 // обе формы вызова updateValue ниже допустимы 👇

 updateValue(1);
 updateValue(previousValue => previousValue + 1);
```

Это может быть полезно, когда обновление состояния зависит от некоторого предыдущего значения состояния.

<Editor code={CounterFnSetState} />
