---
name: useState
route: /usestate
---
import Editor from '../components/Editor'
import {
    DeclareStateVar, 
    UpdateStateVar, 
    MultipleStateVars, 
    StateObject, 
    StateFromFn,
    CounterFnSetState
} from '../components/examples/useState'




# useState 🦄
 
Notes: 
 -`useState` lets you use local state within a function component.
 - Call signature: `const [state, stateUpdaterFunction] = useState(initialStateValue)`
 - View the [docs.](https://reactjs.org/docs/hooks-reference.html#usestate)
 
 Примечания:
  -`useState` позволяет вам использовать локальное состояние внутри функционального компонента.
  - Сигнатура вызова: `const [состояние, stateUpdaterFunction] = useState (initialStateValue)`
  - Просмотрите [документы] (https://reactjs.org/docs/hooks-reference.html#usestate)
--- 

## Declare State Variable 
Declaring a state variable is as simple as calling `useState` with some initial state value.
<Editor code={DeclareStateVar}/>

## Объявить переменную состояния
Объявление переменного состояния является простым вызовом `useState` с некоторым начальным государственным значением.
<Код редактора = {DeclareStateVar} />

## Update State Variable
Updating a state variable is as simple as invoking the updater function returned by the `useState` invocation. 

## Обновить переменную состояния
Обновить переменную состояния так же просто, как вызвать функцию обновления, возвращаемую вызовом useState.

```js
  const [stateValue, updaterFn] = useState(initialStateValue);
```
<Editor code={UpdateStateVar} />

## Multiple State Variables 
Multiple state variables may be used and updated from within a functional component as shown below:

## Несколько переменных состояния
Можно использовать и обновлять несколько переменных состояния из функционального компонента, как показано ниже:

<Editor code={MultipleStateVars} />

## Use Object State Variable
As opposed to strings and numbers, you could also use an object as the initial value passed on to useState.
> Nb: You have to pass the entire object to the `useState` updater function as the object is replaced NOT merged. 

## Использовать переменную состояния объекта
В отличие от строк и чисел, вы также можете использовать объект в качестве начального значения, передаваемого в useState.
> Nb: вы должны передать весь объект в функцию обновления `useState`, поскольку объект заменяется НЕ объединяется.

<Editor code={StateObject} />

```js
// 🐢 setState (object merge) vs useState (object replace)
// assume initial state is {name: "Ohans"}

// 🐢 setState (объединение объектов) vs useState (замена объекта)
// предполагаем, что начальное состояние - {name: "Ohans"}

setState({ age: 'unknown' })
// new state object will be
// {name: "Ohans", age: "unknown"}

useStateUpdater({ age: 'unknown' })
// new state object will be
// {age: "unknown"} - initial object is replaced
 {age: "unknown"} - initial state object is replaced

```
  

## Initialize State from Function 
As opposed to just passing an initial state value, state could also be initialized from a function as shown below:
<Editor code={StateFromFn} />

## Инициализировать состояние из функции
В отличие от простой передачи значения начального состояния, состояние также можно инициализировать из функции, как показано ниже:
<Код редактора = {StateFromFn} />


## Functional setState 
The updater function returned from invoking useState can also take a function similar to the good ol' setState.

## Функциональный setState
Функция обновления, возвращаемая при вызове useState, также может принимать функцию, аналогичную старому доброму setState.

```js
 const [value, updateValue] = useState(0)
 // both forms of invoking updateValue below are valid 👇
 // обе формы вызова updateValue ниже допустимы 👇

 updateValue(1);
 updateValue(previousValue => previousValue + 1);
```
This is ideal when the state update depends on some previous value of state. 

Это идеально, когда обновление состояния зависит от некоторого предыдущего значения состояния.

<Editor code={CounterFnSetState} />
